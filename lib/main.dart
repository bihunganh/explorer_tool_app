import 'package:flutter/material.dart';
import 'explorer_tool.dart'; // Import file mới

void main() {
  runApp(const MaterialApp(
    home: ExplorerTool(),
    debugShowCheckedModeBanner: false,
  ));
}